interface:
  display_name: "Claude Desktop Code Session Restore"
  short_description: "Restore macOS Desktop Code sessions"
  default_prompt: "Use $claude-desktop-code-session-restore to restore prior Claude Desktop Code sessions into my current macOS Claude account."
